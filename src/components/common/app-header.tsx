"use client";

import { Header } from "@/components/ui/vercel-navbar";

/**
 * Primary Application & Landing Header for Nyaya Revolution.
 * Renders the Vercel-inspired 3-column mega-navigation menu (`Header`)
 * with integrated Cmd+K global search, Voice/Action Studio CTAs,
 * live Supabase authentication state, and the 3-way ThemeSwitcher.
 */
export function AppHeader() {
  return <Header />;
}
